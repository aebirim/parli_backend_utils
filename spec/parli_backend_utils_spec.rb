require "spec_helper"

describe ParliBackendUtils do
  it "has a version number" do
    expect(ParliBackendUtils::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
